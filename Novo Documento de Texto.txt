Algoritmo
	declare literal op, a;
	numerico s, d;
	escreva("s + a");
	leia(op);
	se(op = falso) inicio
		escreva("Oi, eu sou o Goku!");
	fim
	senao inicio
		escreva("Seu inseto, achei que iria preferir o Kakaroto");
	fim
fim algoritmo

subrotina CmdRepita(v2 numerico)
    se (num1 > num2) inicio
        t <-- 5 * 6 + 8 / 2;
    fim
    repita
        cmd <-- "grelhado" + t;
    ate 8 ou 80

    j(valeska, teles, anta);

fim subrotina